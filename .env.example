DATABASE_URL=postgres://blokm:blokm@localhost:5432/blokm?sslmode=disable
API_ADDR=:8080
AUTO_SEED=1
# Optional. If set, GET /admin/suggestions requires header X-Admin-Token.
# ADMIN_TOKEN=

# Frontend (copy into web/.env.local)
NEXT_PUBLIC_API_URL=http://localhost:8080
# Optional. When unset, the map uses Leaflet + Carto/OSM tiles (no key required).
# NEXT_PUBLIC_MAPBOX_TOKEN=
